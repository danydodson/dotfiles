# bat.conf

# disable automatic paging:
--paging=never

# syntax mappings
--map-syntax "*.ino:C++"
--map-syntax "ignore:Git Ignore"

# Run `bat --list-themes` for a list of all available themes
--theme="OneHalfDark" # or TwoDark

# enable italic text on the terminal
# --italic-text=always
