```bash
# --- // INSTALL_PKGFILE:
sudo pacman -S pkgfile
```

```bash
# --- // SYSTEMD_PKGFILE_UPDATE_TIMER:
sudo systemctl enable pkgfile-update.timer
sudo systemctl start pkgfile-update.timer
```


